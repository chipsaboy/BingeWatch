<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'BingeWatch', root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">Watchlist <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="#">Add a show</a></li>
              <li><a href="#">View Watchlist</a></li>
              <li><a href="#">Edit your Watchlist</a></li>
            </ul>
          </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if !current_user %>
          <li>
            <%= link_to new_user_registration_path do %>
                Sign Up
                <span class="glyphicon glyphicon-user"></span>
            <% end %>
            </li>
          <li>
            <%= link_to new_user_session_path do %>
                Log In
                <span class="glyphicon glyphicon-log-in"></span>
            <% end %>
          </li>
        <% else %>
          <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= current_user.name %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#">Page 1-1</a></li>
            <li><a href="#">Page 1-2</a></li>
            <li><%= link_to "Log Out", destroy_user_session_path %></li>
          </ul>
        <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>